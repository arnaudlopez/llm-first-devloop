version: 2
goal:
  slug: "devloop-skill-loop-default"
  title: "DevLoop Skill Loop Default"
  status: active
  created_at: "2026-05-24"
  updated_at: "2026-05-24"
  kind: specific
  original_request: "# DevLoop Skill Loop Default\n\n## Intent\n\nAlign the Codex `llm-first-devloop` skill with the new product direction: `loop` is the default guided mode, while `run` remains a lower-level primitive.\n\nWhen the owner says \"next\", \"enchaine\", \"use DevLoop\", or asks to continue a board, the skill should guide Codex to invoke `llm-first-devloop loop`, surface the board URL, and keep advancing with receipts until blocked or complete.\n\n- Do not remove `run`.\n- Do not launch native `/goal` automatically.\n- Do not build a fully autonomous runtime yet.\n- Do not weaken repo guard, board URL, blocker, or receipt requirements.\n\n## Non-Goals\n\n- TODO: Name what this goal must not change.\n\n## Proposed Oracle\n\nThe repository skill and installed Codex skill both document loop as the default guided command, tests prove this expectation, sync-skill installs it, and CI passes.\n\n## Suggested Mode\n\nimplementation\n\n## Acceptance Hints\n\n- `skills/llm-first-devloop/SKILL.md` tells Codex to use `llm-first-devloop loop` by default.\n- The skill still explains `run` as a lower-level primitive/debug fallback.\n- The installed skill under `~/.codex/skills/llm-first-devloop/SKILL.md` is synced.\n- Tests assert the skill defaults to `loop` and preserves board/repo/blocker behavior.\n- `npm test` passes.\n\n## Risks And Open Questions\n\n- TODO: List ambiguity, missing credentials, operational risks, or decisions needed before implementation.\n\n## Constraints\n\n- TODO: Capture constraints, must-preserve behavior, boundaries, or forbidden changes.\n\n## Ready Mode Command\n\n```bash\nnpm run ready -- --from ./docs/goals/devloop-skill-loop-default/brief.md --mode implementation --oracle \"The repository skill and installed Codex skill both document loop as the default guided command, tests prove this expectation, sync-skill installs it, and CI passes.\" --out docs/goals/devloop-skill-loop-default\n```\n\n## Source Notes\n\nCompiled from: /Users/arnaud/Documents/llm-first-devloop/docs/devloop-skill-loop-default-notes.md\n\n> # DevLoop Skill Loop Default\n> \n> ## Intent\n> \n> Align the Codex `llm-first-devloop` skill with the new product direction: `loop` is the default guided mode, while `run` remains a lower-level primitive.\n> \n> When the owner says \"next\", \"enchaine\", \"use DevLoop\", or asks to continue a board, the skill should guide Codex to invoke `llm-first-devloop loop`, surface the board URL, and keep advancing with receipts until blocked or complete.\n> \n> ## Non-Goals\n> \n> - Do not remove `run`.\n> - Do not launch native `/goal` automatically.\n> - Do not build a fully autonomous runtime yet.\n> - Do not weaken repo guard, board URL, blocker, or receipt requirements.\n> \n> ## Proposed Oracle\n> \n> The repository skill and installed Codex skill both document `loop` as the default guided command, tests prove this expectation, sync-skill installs it, and CI passes.\n> \n> ## Acceptance\n> \n> - `skills/llm-first-devloop/SKILL.md` tells Codex to use `llm-first-devloop loop` by default.\n> - The skill still explains `run` as a lower-level primitive/debug fallback.\n> - The installed skill under `~/.codex/skills/llm-first-devloop/SKILL.md` is synced.\n> - Tests assert the skill defaults to `loop` and preserves board/repo/blocker behavior.\n> - `npm test` passes."
  interpreted_outcome: "Run the prepared LLM-first workflow for DevLoop Skill Loop Default."
  input_shape: specific
  audience: "Product owner and target users"
  authority: requested
  proof_type:
    - test
    - artifact
    - review
  completion_proof: "Final audit maps red tests, green verification, implementation receipts, and shipping proof back to the oracle."
  goal_oracle: "The repository skill and installed Codex skill both document loop as the default guided command, tests prove this expectation, sync-skill installs it, and CI passes."
  oracle:
    signal: "The repository skill and installed Codex skill both document loop as the default guided command, tests prove this expectation, sync-skill installs it, and CI passes."
    final_proof: "T999 states full_outcome_complete: true and cites passing acceptance evidence, verification commands, design review, and T998 shipping evidence."
  likely_misfire: "Starting implementation from a plausible plan before converting the owner outcome into acceptance tests and observable proof."
  constraints:
    - "Acceptance tests or equivalent observable proof must be written before production implementation."
    - "Implementation must stay inside Judge-approved allowed_files."
    - "Unrelated dirty worktree changes must remain untouched."

rules:
  pm_owns_state: true
  one_active_task: true
  max_write_workers: 1
  no_implementation_without_worker_or_pm_task: true
  no_completion_without_judge_or_pm_audit: true
  planning_is_not_completion: true
  queued_required_worker_blocks_completion: true
  continuous_until_full_outcome: true
  missing_input_or_credentials_do_not_stop_goal: true
  preserve_and_validate_existing_plan: true
  intake_misfire_must_be_audited: true
  allow_read_only_goal_without_shipping: true
  require_quality_checker: true
  require_tdd_worker_flow: true
  require_github_ship_task: true
  require_impact_assessment: true
  goal_pressure_requires_oracle: true

goal_policy:
  mode: implementation
  requires_tdd: true
  requires_impact_assessment: true
  requires_shipping: true
  requires_visual_verification: false
  requires_migration_proof: false
  requires_infra_proof: false
  requires_dependency_review: false
  requires_dirty_worktree_guard: true
  requires_refactor_proof: false
  rationale: "Ready Mode is for app-code or product-behavior work after an LLM-first spec conversation has converged."

workflow_safeguards:
  requires_design_concept: true
  requires_ubiquitous_language: true
  requires_feedback_policy: true
  requires_test_strategy: true
  requires_module_map: true
  requires_interface_contract: true
  requires_architecture_review: true
  requires_design_delta: true
  require_t998_shipping: true
  require_t999_final_audit: true
  protect_unrelated_dirty_worktree: true

agents:
  scout: installed
  worker: installed
  judge: installed

active_task: T998
tasks:
  - id: T001
    type: scout
    assignee: Scout
    status: done
    objective: "Turn the mature request into a concrete acceptance contract before any implementation work."
    inputs:
      - "goal.md"
      - "acceptance-contract.md"
      - "Current repository source, tests, scripts, docs, and dirty worktree status"
    constraints:
      - "Read-only."
      - "Do not edit product files."
      - "Do not start implementation."
      - "Prefer observable owner outcomes over internal task completion."
    expected_output:
      - "design_concept: shared understanding, non-goals, and decision dependencies"
      - "ubiquitous_language: domain terms, overloaded words, and forbidden synonyms"
      - "module_map: relevant deep modules, boundaries, and shallow-module risks"
      - "interface_contract: public behavior and boundaries that tests should target"
      - "test_strategy: acceptance tests, unit/integration boundaries, mocks, browser or visual checks if relevant"
      - "feedback_policy: targeted test cadence, typecheck, browser checks, and full-suite timing"
      - "acceptance_contract_updates: exact end-state evidence and failure modes"
      - "dirty_worktree_risk_map: unrelated files that must stay untouched"
      - "impact_assessment covering db_schema_migrations, data_backfill, env_secrets, auth_permissions, api_contract, ui_routes, background_jobs, external_services, deploy_rollback, observability, and docs"
    receipt:
      result: done
      summary: "Mapped the repo and installed llm-first-devloop skill. Both still instruct Codex to use run as the default command, while the product now makes loop the default guided mode. Existing tests only verify board/repo/blocker safety, not loop-default behavior."
      design_concept: "The skill should make loop the default operator path after LLM-first intent is mature. run remains documented as a lower-level primitive/debug fallback."
      ubiquitous_language:
        loop: "Default guided DevLoop command that wraps run and emits safe action, stop rules, and receipt template."
        run: "Lower-level entry primitive for interview/ready/check/next handoff."
        handoff: "The CLI output that includes repo, board, active task, safe action and prompt."
      module_map:
        target_files:
          - skills/llm-first-devloop/SKILL.md
          - scripts/llm-first-devloop-skill.test.mjs
          - "docs/goals/devloop-skill-loop-default/**"
          - docs/devloop-skill-loop-default-notes.md
        install_target: "/Users/arnaud/.codex/skills/llm-first-devloop/SKILL.md via sync-skill"
      interface_contract:
        default_command: "llm-first-devloop loop --state ... or --from ... --out ... --oracle ..."
        fallback_command: "llm-first-devloop run for low-level/debug use only"
      test_strategy:
        red: "Update skill test to expect loop default and run fallback wording."
        green: "Edit repo skill, run sync-skill, verify installed copy, npm test."
      feedback_policy: "Run node --test scripts/llm-first-devloop-skill.test.mjs for red/green, then npm test and git diff --check."
      dirty_worktree_risk_map: "Only the new notes and board are dirty before this goal. Previous repo state was clean."
      impact_assessment:
        db_schema_migrations: none
        data_backfill: none
        env_secrets: none
        auth_permissions: none
        api_contract: none
        ui_routes: none
        background_jobs: none
        external_services: none
        deploy_rollback: "git revert"
        observability: "CLI/skill tests and sync-skill output"
        docs: "skill documentation changes"
  - id: T002
    type: judge
    assignee: Judge
    status: done
    objective: "Approve the acceptance contract and choose the largest safe red-test package."
    inputs:
      - "T001 receipt"
      - "Updated acceptance-contract.md if T001 changed it"
    constraints:
      - "Do not implement."
      - "Reject implementation work before tests can express the oracle."
      - "Reject vague acceptance criteria that cannot be verified."
      - "Choose a useful vertical slice, not tiny helper-only work."
    expected_output:
      - "Decision: approved | needs_more_evidence | blocked"
      - "Approved design_concept"
      - "Approved ubiquitous_language"
      - "feedback_policy"
      - "test_strategy"
      - "module_map"
      - "interface_contract"
      - "architecture_review"
      - "Exact Worker objective for red tests"
      - "allowed_files for T003"
      - "verify commands for T003"
      - "stop_if conditions for T003"
      - "impact_assessment_ref"
    receipt:
      result: done
      decision: approved
      summary: "Approved a small TDD slice to align the Codex skill with loop as default and keep run as fallback."
      architecture_review: "Use the existing skill file and sync-skill mechanism. Do not add runtime behavior; this is instruction/test alignment plus install sync."
      task_updates:
        T003:
          allowed_files:
            - scripts/llm-first-devloop-skill.test.mjs
            - "docs/goals/devloop-skill-loop-default/**"
          verify:
            - "node --test scripts/llm-first-devloop-skill.test.mjs"
          stop_if:
            - "Need implementation changes before red evidence."
            - "Need files outside allowed_files."
            - "No meaningful failing test can be written."
            - "Need DB migration, schema change, backfill, env/secret, auth/RLS/permission, external service, background job, deploy, or rollback work outside allowed_files."
          impact_assessment_ref: T001.receipt.impact_assessment
        T004:
          allowed_files:
            - skills/llm-first-devloop/SKILL.md
            - scripts/llm-first-devloop-skill.test.mjs
            - "docs/goals/devloop-skill-loop-default/**"
            - docs/devloop-skill-loop-default-notes.md
          verify:
            - "node --test scripts/llm-first-devloop-skill.test.mjs"
            - "node scripts/llm-first-devloop.mjs sync-skill --json"
            - "npm test"
          stop_if:
            - "Skill stops surfacing Board URL or Repo guard."
            - "Need native /goal automatic launch."
            - "Need files outside allowed_files."
            - "Need DB migration, schema change, backfill, env/secret, auth/RLS/permission, external service, background job, deploy, or rollback work outside allowed_files."
          impact_assessment_ref: T001.receipt.impact_assessment
        T005:
          allowed_files:
            - skills/llm-first-devloop/SKILL.md
            - scripts/llm-first-devloop-skill.test.mjs
            - "docs/goals/devloop-skill-loop-default/**"
            - docs/devloop-skill-loop-default-notes.md
          verify:
            - "node --test scripts/llm-first-devloop-skill.test.mjs"
            - "npm test"
            - "git diff --check"
          stop_if:
            - "Verification fails twice without understood cause."
            - "Need files outside allowed_files."
          impact_assessment_ref: T001.receipt.impact_assessment
  - id: T003
    type: worker
    worker_kind: red_test
    assignee: Worker
    status: done
    objective: "Write the failing acceptance tests selected by Judge, without production implementation."
    inputs:
      - "architecture_review"
      - "interface_contract"
      - "module_map"
      - "test_strategy"
      - "feedback_policy"
      - "ubiquitous_language"
      - "design_concept"
      - "T002 approved acceptance contract"
      - "T002 allowed_files and verify commands"
    impact_assessment_ref: T001.receipt.impact_assessment
    allowed_files:
      - scripts/llm-first-devloop-skill.test.mjs
      - "docs/goals/devloop-skill-loop-default/**"
    verify:
      - "node --test scripts/llm-first-devloop-skill.test.mjs"
    stop_if:
      - "Need implementation changes before red evidence."
      - "Need files outside allowed_files."
      - "No meaningful failing test can be written."
      - "Need DB migration, schema change, backfill, env/secret, auth/RLS/permission, external service, background job, deploy, or rollback work outside allowed_files."
    receipt:
      result: done
      summary: "Added a failing skill test requiring loop as the default guided command while preserving run as a lower-level fallback."
      changed_files:
        - scripts/llm-first-devloop-skill.test.mjs
      red_evidence:
        command: "node --test scripts/llm-first-devloop-skill.test.mjs"
        result: "failed as expected"
        failure: "Skill still documents llm-first-devloop run as default and does not contain llm-first-devloop loop --state."
      verify:
        -
          command: "node --test scripts/llm-first-devloop-skill.test.mjs"
          result: expected_failure
      impact_assessment_ref: T001.receipt.impact_assessment
  - id: T004
    type: worker
    worker_kind: implementation
    assignee: Worker
    status: done
    objective: "Implement the largest safe useful slice that makes the approved red tests pass."
    inputs:
      - "architecture_review"
      - "interface_contract"
      - "module_map"
      - "test_strategy"
      - "feedback_policy"
      - "ubiquitous_language"
      - "design_concept"
      - "T003 red test receipt"
      - "T002 implementation boundaries"
    impact_assessment_ref: T001.receipt.impact_assessment
    allowed_files:
      - skills/llm-first-devloop/SKILL.md
      - scripts/llm-first-devloop-skill.test.mjs
      - "docs/goals/devloop-skill-loop-default/**"
      - docs/devloop-skill-loop-default-notes.md
    verify:
      - "node --test scripts/llm-first-devloop-skill.test.mjs"
      - "node scripts/llm-first-devloop.mjs sync-skill --json"
      - "npm test"
    stop_if:
      - "Skill stops surfacing Board URL or Repo guard."
      - "Need native /goal automatic launch."
      - "Need files outside allowed_files."
      - "No meaningful failing test can be written."
      - "Need DB migration, schema change, backfill, env/secret, auth/RLS/permission, external service, background job, deploy, or rollback work outside allowed_files."
    receipt:
      result: done
      summary: "Updated the repository DevLoop skill to use loop as the default guided command, preserve run as the lower-level fallback, and synced the installed Codex skill."
      changed_files:
        - skills/llm-first-devloop/SKILL.md
        - scripts/llm-first-devloop-skill.test.mjs
      green_test:
        command: "node --test scripts/llm-first-devloop-skill.test.mjs"
        result: pass
      sync:
        command: "node scripts/llm-first-devloop.mjs sync-skill --json"
        result: copied
        targetPath: /Users/arnaud/.codex/skills/llm-first-devloop/SKILL.md
      verify:
        -
          command: "node --test scripts/llm-first-devloop-skill.test.mjs"
          result: pass
        -
          command: "node scripts/llm-first-devloop.mjs sync-skill --json"
          result: pass
      impact_assessment_ref: T001.receipt.impact_assessment
  - id: T005
    type: worker
    worker_kind: verification
    assignee: Worker
    status: done
    objective: "Run the full verification package, including browser or visual checks when the acceptance contract requires them."
    inputs:
      - "architecture_review"
      - "interface_contract"
      - "module_map"
      - "test_strategy"
      - "feedback_policy"
      - "ubiquitous_language"
      - "design_concept"
      - "T004 implementation receipt"
      - "Approved feedback_policy"
    impact_assessment_ref: T001.receipt.impact_assessment
    allowed_files:
      - skills/llm-first-devloop/SKILL.md
      - scripts/llm-first-devloop-skill.test.mjs
      - "docs/goals/devloop-skill-loop-default/**"
      - docs/devloop-skill-loop-default-notes.md
    verify:
      - "node --test scripts/llm-first-devloop-skill.test.mjs"
      - "npm test"
      - "git diff --check"
    stop_if:
      - "Verification fails twice without understood cause."
      - "Need files outside allowed_files."
      - "No meaningful failing test can be written."
      - "Need DB migration, schema change, backfill, env/secret, auth/RLS/permission, external service, background job, deploy, or rollback work outside allowed_files."
    receipt:
      result: done
      summary: "Verification passed for the loop-default skill update."
      changed_files:
        []
      verification:
        -
          command: "node --test scripts/llm-first-devloop-skill.test.mjs"
          result: pass
          tests: "1 pass"
        -
          command: "npm test"
          result: pass
          tests: "66 pass"
        -
          command: "git diff --check"
          result: pass
        -
          command: "rg -n \"llm-first-devloop loop|LOOP_READY|state_outside_repo\" /Users/arnaud/.codex/skills/llm-first-devloop/SKILL.md"
          result: pass
      verify:
        -
          command: "node --test scripts/llm-first-devloop-skill.test.mjs && npm test && git diff --check"
          result: pass
      impact_assessment_ref: T001.receipt.impact_assessment
  - id: T006
    type: judge
    assignee: Judge
    status: done
    objective: "Review architecture, spec fidelity, and test evidence before shipping."
    inputs:
      - "T001 through T005 receipts"
      - "Current diff"
      - "Last verification output"
    constraints:
      - "Do not implement."
      - "Reject completion if the implementation only satisfies internal tasks but not the original oracle."
      - "Reject hidden design drift, shallow-module sprawl, or missing acceptance evidence."
    expected_output:
      - "approved | changes_required"
      - "design_delta"
      - "architecture_review"
      - "oracle_coverage"
      - "remaining risks"
      - "whether another Worker slice is needed"
    receipt:
      result: done
      decision: approved
      summary: "Skill alignment approved. The repo and installed Codex skill now use loop as the default guided command, while run remains a fallback primitive."
      design_delta: "Codex skill moved from run/HANDOFF_READY as default to loop/LOOP_READY as default, including safeAction, stopRules and receiptTemplate in the task instructions."
      architecture_review: "Approved. No runtime behavior was added; this is skill instruction alignment plus a test that prevents regression. sync-skill remains the installation mechanism."
      oracle_coverage:
        repo_skill_loop_default: true
        installed_skill_synced: true
        tests_prove_expectation: true
        run_fallback_preserved: true
      remaining_risks:
        - "The current tranche still guides Codex; it does not automatically launch native /goal or execute a fully autonomous daemon."
      another_worker_slice_needed: false
  - id: T998
    type: pm
    assignee: PM
    status: active
    objective: "Commit all goal-related changes and push the current branch once verification is green."
    inputs:
      - "Approved implementation and verification receipts"
      - "Current git status"
    constraints:
      - "Do not include unrelated dirty files."
      - "If the workspace is not a git repository, record shipping_blocker: no_git_repository."
      - "If no GitHub remote exists, record shipping_blocker: no_github_remote."
    expected_output:
      - "Or shipping_blocker: no_git_repository | no_github_remote"
      - "Unrelated dirty files left untouched"
      - "Committed files"
      - "Push result"
      - "Remote branch"
      - "Commit SHA"
      - "commit_sha"
      - "remote_branch or push string"
      - "push_result"
      - "committed_files"
      - "unrelated_dirty_files_left_untouched"
    receipt: null
  - id: T999
    type: judge
    assignee: Judge
    status: queued
    reasoning_hint: high
    objective: "Audit whether the full original outcome is complete."
    inputs:
      - "design_concept, ubiquitous_language, feedback_policy, test_strategy, module_map, interface_contract, architecture_review, and design_delta evidence"
      - "All done task receipts"
      - "Last verification"
      - "Current dirty diff"
      - "Quality checker final output"
      - "Completed impact_assessment receipt"
      - "T998 shipping proof when shipping is required"
    constraints:
      - "Reject completion if design_concept, ubiquitous_language, feedback_policy, test_strategy, module_map, interface_contract, architecture_review, or design_delta evidence is missing when workflow_safeguards requires it."
      - "Do not implement."
      - "Reject completion if required Worker work is still queued or active."
      - "Reject completion unless impact_assessment covers db_schema_migrations, data_backfill, env_secrets, auth_permissions, api_contract, ui_routes, background_jobs, external_services, deploy_rollback, observability, and docs when impact assessment is required."
      - "For read-only audit goals with no Worker tasks and no changed_files, accept audit_scope/audit_findings/review/decision evidence instead of TDD, impact_assessment, T998, commit, and push."
      - "For verification goals, reject completion unless safe test/browser command evidence exists and no changed_files are reported."
      - "Reject completion unless T998 has a commit SHA and successful push result, or an explicit shipping_blocker for no_git_repository/no_github_remote, when shipping is required."
    expected_output:
      - "design_delta"
      - "workflow_safeguards result"
      - "complete | not_complete"
      - "full_outcome_complete: true | false"
      - "missing evidence"
      - "next task if not complete"
    receipt: null
